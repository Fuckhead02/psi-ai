# Railway.app Deployment Guide

## Step 1: Create a GitHub Repository

1. Go to https://github.com/new
2. Create a new repository named `psychologist-ai`
3. Clone it locally or use GitHub's web interface

## Step 2: Push Code to GitHub

```bash
# Initialize git in the project
git init
git add .
git commit -m "Initial commit: Psychologist AI Chatbot"
git branch -M main
git remote add origin https://github.com/Fuckhead02/psychologist-ai.git
git push -u origin main
```

## Step 3: Deploy to Railway

1. **Go to Railway.app:** https://railway.app/
2. **Sign in with GitHub** using your Fuckhead02 account
3. **Create a new project** → "Deploy from GitHub repo"
4. **Select your repository:** `psychologist-ai`
5. **Configure environment variables:**

```
DATABASE_URL=mysql://user:password@host/dbname
JWT_SECRET=your-secret-key-here
VITE_APP_ID=your-app-id
OAUTH_SERVER_URL=https://api.manus.im
VITE_OAUTH_PORTAL_URL=https://oauth.manus.im
OWNER_OPEN_ID=your-owner-id
OWNER_NAME=Your Name
BUILT_IN_FORGE_API_URL=https://api.manus.im
BUILT_IN_FORGE_API_KEY=your-api-key
VITE_FRONTEND_FORGE_API_KEY=your-frontend-key
VITE_FRONTEND_FORGE_API_URL=https://api.manus.im
VITE_ANALYTICS_ENDPOINT=https://analytics.manus.im
VITE_ANALYTICS_WEBSITE_ID=your-website-id
VITE_APP_TITLE=Psychologist AI
VITE_APP_LOGO=🌙
```

6. **Add MySQL Plugin:**
   - In Railway dashboard, click "Add Plugin"
   - Select "MySQL"
   - Railway will automatically set `DATABASE_URL`

7. **Deploy:**
   - Railway will automatically build and deploy
   - Your app will be live at a Railway URL

## Step 4: Custom Domain (Optional)

1. In Railway dashboard, go to Settings
2. Add your custom domain
3. Configure DNS records as shown

## Environment Variables Notes

- **DATABASE_URL**: Will be auto-generated by Railway's MySQL plugin
- **JWT_SECRET**: Generate a random string (e.g., `openssl rand -base64 32`)
- **API Keys**: Use the same keys from your Manus deployment
- **VITE_* variables**: Frontend environment variables

## Build Command

Railway will automatically run:
```bash
pnpm build
```

## Start Command

Railway will automatically run:
```bash
npm start
```

## Troubleshooting

- **Build fails**: Check that all dependencies are in `package.json`
- **Database connection fails**: Verify `DATABASE_URL` is set correctly
- **App crashes**: Check Railway logs in the dashboard

## Support

For Railway support: https://railway.app/support
For app issues: Check the logs in Railway dashboard

---

**Your app will be live once Railway completes the build and deployment!** 🚀
